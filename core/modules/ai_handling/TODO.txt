// TODO & PLAN:

Aufruf:
psy = [thisTrigger,["basic",300]] spawn ai_handler;													Basic mit random patrol, area 300m als standard task
psy = [thisTrigger,["basic",50,"defend"]] spawn ai_handler;											Basic mit defend, mit random pos um das center
psy = [thisTrigger,["motor_basic",200,["repsawn",3]], ["basic",400,"defend"]] spawn ai_handler;		motorisierte inf spawnt noch 3 mal neu und eine basic gruppe, verteidigung


psy = [["MBT",300]] spawn ai_handler; 													spawnt 1 Panzer, Radius 300m, Task defend
psy = [["MBT",300,2,"defend"]] spawn ai_handler; 										spawnt 2 Panzer, Radius 300m, Task defend

-----------------------------------------------------------------------------------------Spawn Typen: (als String)
basic					// normale gruppe
specops					// kleine specops gruppe mit mehr skill

motor_basic				// auto und 3x fußsoldat
motor_at				// auto und 3x fußsoldat
motor_aa				// auto und 3x fußsoldat

mech_basic				// apc spezialisiert und eine komplette gruppe
mech_at					// apc spezialisiert und eine komplette gruppe
mech_aa					// apc spezialisiert und eine komplette gruppe	- vollwertige AA stellung
mech_support			// apc spezialisiert und eine komplette gruppe

tank_section			// 2 main battle tanks in einer Gruppe
apc_section				// 2 apc's in einer Gruppe (wirklich APC?! eher mobile Arty)
tank_apc_platoon		// ein kombinierter Zug aus 4 schweren Fahrzeugen (mit AA)

mbt
apc_tracked
apc_wheeled
aa
car
car_armed
static_light
static_heavy
static_mortar
supply_fuel
supply_rep
supply_ammo




-----------------------------------------------------------------------------------------Spawn Typen: (als String)
"defend"					// gebiet halten, per task defend		done
"patrol"					// random waypoints						done
["attack",thereObj]			// definierten punkt angreifen, alternativ in die richtung des triggers eine angriffsbewegung durchführen
["attack","thereMarker"]	// definierten punkt angreifen, alternativ in die richtung des triggers eine angriffsbewegung durchführen
["respawn",3]				// die gruppe mit diesem task verteidigt ein gebiet, die neu gespawnte gruppe greift die pos an (ausgehend von der annahme der feind besitzt diese pos jetzt
["respawn",3,thereObj]		// die gruppe mit diesem task verteidigt ein gebiet, die neu gespawnte gruppe greift die pos an (ausgehend von der annahme der feind besitzt diese pos jetzt) + spawnPos






- Patrolradius ist 25% größer als der Spawnradius bei Inf
- Patrolradius ist 50% größer als der Spawnradius bei Vecs

TODO bei public stand-alone release:
- prüfen ob alle common functions enthalten sind (arrayoushstack2, tcb_fnc_RandomArrayVal)
- nethandling vorhanden?